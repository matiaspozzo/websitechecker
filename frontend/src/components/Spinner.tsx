export function Spinner() {
  return (
    <span
      role="status"
      aria-label="loading"
      className="inline-block h-3 w-3 shrink-0 animate-spin rounded-full border-2 border-ink-muted border-t-accent"
    />
  )
}
